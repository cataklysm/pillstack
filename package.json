{
  "name": "pillstack",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first medication and supplement manager",
  "workspaces": ["contracts", "app", "web"],
  "scripts": {
    "dev": "npm run dev --workspace=app & npm run dev --workspace=web",
    "dev:app": "npm run dev --workspace=app",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=contracts && npm run build --workspace=app && npm run build --workspace=web",
    "test": "npm run test --workspace=app",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": { "node": ">=22" }
}
